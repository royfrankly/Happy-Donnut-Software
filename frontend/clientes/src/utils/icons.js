// src/utils/icons.js
import { Mail, Lock, User as UserIcon } from 'lucide-react';

// Opcional: Si prefieres seguir usando los SVG personalizados, usa estos componentes
// Pero por simplicidad y coherencia, usaremos Lucide (como en tu App original)

export { Mail as MailIcon, Lock as LockIcon, UserIcon };